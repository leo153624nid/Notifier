version: "2"

run:
  timeout: 3m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - unused
    - ineffassign
    - bodyclose
    - sqlclosecheck
  settings:
    govet:
      enable-all: true

formatters:
  enable:
    - gofmt
    - goimports